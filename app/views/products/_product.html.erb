<article class="product">
  <header>
    <%= link_to product do %>
      <%= image_tag product.image.thumb, alt: product.name %>
      <h4><%= product.name %></h4>
    <% end %>
    <% if product.overall_rating == 0 %>
      <h5 class="no-rating-tag">No rating yet</h5>
    <% else %>
      <% for i in 1..product.overall_rating do %>
        <span>⭐️</span>
      <% end %>
    <% end %>
  </header>
  <p class="description">
    <%= product.description %>
  </p>
  <footer class="actions">
    <%= button_to add_item_cart_path(product_id: product.id), class: 'btn btn-primary' , method: :post do %>
      <%= fa_icon "shopping-cart", text: 'Add' %>
    <% end %>
    <%= link_to product, class: 'btn btn-default pull-right' do %>
      Details &raquo;
    <% end %>
  </footer>

  <% if product.quantity == 0 %>
    <aside class="sold-out-tag">
        SOLD OUT!
    </aside>
  <% end %>

  <aside class="price">
    <%= humanized_money_with_symbol product.price %>
  </aside>
</article>
